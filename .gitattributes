# Keep shell scripts, SQL and CI YAML LF so they run on Linux CI regardless of
# the checkout platform.
*.sh   text eol=lf
*.sql  text eol=lf
*.mjs  text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
Makefile text eol=lf
