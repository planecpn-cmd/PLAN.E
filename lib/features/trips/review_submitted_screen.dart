// RM-15 Review Submitted
import 'package:flutter/material.dart';

class ReviewSubmittedScreen extends StatelessWidget {
  const ReviewSubmittedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RM-15 Review Submitted')),
      body: const Center(
        child: Text('RM-15 Review Submitted Confirmation Screen'),
      ),
    );
  }
}
