// RM-04 Password Reset Result
import 'package:flutter/material.dart';

class ResetResultScreen extends StatelessWidget {
  const ResetResultScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RM-04 Reset Result')),
      body: const Center(
        child: Text('RM-04 Password Reset Result Screen'),
      ),
    );
  }
}
