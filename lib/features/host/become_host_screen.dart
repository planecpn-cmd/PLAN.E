// PL-18 Become a Host
import 'package:flutter/material.dart';

class BecomeHostScreen extends StatelessWidget {
  const BecomeHostScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PL-18 Become a Host')),
      body: const Center(
        child: Text('PL-18 Become a Host Landing Screen'),
      ),
    );
  }
}
