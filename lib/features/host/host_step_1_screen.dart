// RM-22 Host Application Step 1
import 'package:flutter/material.dart';

class HostStep1Screen extends StatelessWidget {
  const HostStep1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RM-22 Host Step 1')),
      body: const Center(
        child: Text('RM-22 Host Application Step 1 (Info) Screen'),
      ),
    );
  }
}
